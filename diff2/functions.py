#one very useful feature of python is function
def functionName(parameter,parameters):
    print(parameter)
    variable = "declaration"
    #this variable stays in the function as it is a local variable in the scope of the function
    #you can return values
    return "a string"
useOfFunction = functionName("","")
#will return "a string" assign the "useofFunction" variable to "a string"

